# 开发规范文档

本文档定义了项目的开发规范和最佳实践，确保生成的代码符合规范，便于后续开发人员接手。

## 📋 目录

- [代码规范](#代码规范)
- [组件规范](#组件规范)
- [样式规范](#样式规范)
- [数据管理规范](#数据管理规范)
- [类型定义规范](#类型定义规范)
- [路由规范](#路由规范)

## 代码规范

### 1. TypeScript 严格模式

- 所有文件必须使用 TypeScript
- 禁止使用 `any` 类型，必须定义明确的类型
- 使用接口（interface）定义对象类型
- 使用类型别名（type）定义联合类型或复杂类型

```typescript
// ✅ 正确
interface User {
  id: string;
  name: string;
}

// ❌ 错误
const user: any = { id: '1', name: '张三' };
```

### 2. 文件命名

- 组件文件使用 PascalCase：`UserProfile.tsx`
- 工具函数文件使用 camelCase：`formatDate.ts`
- 类型定义文件使用 camelCase：`userTypes.ts`
- 页面文件使用 PascalCase：`HomePage.tsx`

### 3. 导入顺序

```typescript
// 1. React 相关
import { useState } from 'react';

// 2. 第三方库
import { Button } from '@/components/ui/button';

// 3. 项目内部模块
import { useAppStore } from '@/store/useAppStore';
import { MOCK_USERS } from '@/lib/mock/example';

// 4. 类型定义
import type { User } from '@/types';
```

## 组件规范

### 1. 组件结构

```typescript
// 1. 导入
import { useState } from 'react';
import { Button } from '@/components/ui/button';

// 2. 类型定义
interface ComponentProps {
  title: string;
  onAction?: () => void;
}

// 3. Mock 数据（如果需要）
// --- Mock Data ---
const MOCK_DATA = { ... };

// 4. 组件实现
export function Component({ title, onAction }: ComponentProps) {
  // 状态
  const [state, setState] = useState();

  // 事件处理
  const handleClick = () => {
    // TODO: [Dev] 处理点击逻辑
  };

  // 渲染
  return (
    <div>
      <h1>{title}</h1>
      <Button onClick={handleClick}>操作</Button>
    </div>
  );
}
```

### 2. 组件分类

- **UI 组件** (`src/components/ui/`): shadcn/ui 基础组件，不要修改
- **业务组件** (`src/components/biz/`): 跨页面复用的业务组件
- **页面组件** (`src/pages/[FeatureName]/components/`): 页面独有的组件

### 3. 组件复用

- **优先使用 shadcn/ui 组件**，不要重复造轮子
- 如果 shadcn/ui 没有，先检查是否有现成的业务组件
- 只有在确实需要时才创建新组件

## 样式规范

### 1. 使用 Tailwind CSS

- **严禁**创建 `.css` 或 `.scss` 文件
- **严禁**使用 `style={{}}` 行内样式
- 所有样式必须使用 Tailwind CSS 工具类

```typescript
// ✅ 正确
<div className="flex flex-col gap-4 p-6 bg-background">

// ❌ 错误
<div style={{ display: 'flex', padding: '24px' }}>
```

### 2. 颜色使用

- **必须使用语义化颜色变量**，支持深色模式
- 禁止使用硬编码颜色值

```typescript
// ✅ 正确
<div className="bg-primary text-primary-foreground">
<div className="bg-muted text-muted-foreground">

// ❌ 错误
<div className="bg-blue-500 text-white">
```

### 3. 布局规范

- 页面容器统一使用：`flex flex-col gap-4 p-6`
- 响应式设计：默认优先考虑桌面端（Desktop First）

## 数据管理规范

### 1. Mock 数据分离

**严禁在 JSX 中写死数据**，必须将数据分离出来：

```typescript
// ❌ 错误
<div className="text-xl">张三</div>

// ✅ 正确
const MOCK_USER = { name: '张三' };
<div className="text-xl">{MOCK_USER.name}</div>
```

### 2. Mock 数据位置

- **简单页面**：在文件顶部定义 `// --- Mock Data ---` 区域
- **复杂页面**：提取到 `src/lib/mock/[feature].ts`

```typescript
// 简单页面示例
export default function SimplePage() {
  // --- Mock Data ---
  const pageTitle = '页面标题';
  const items = ['项目1', '项目2'];
  
  return <div>...</div>;
}
```

### 3. TODO 标记

在所有需要开发人员对接的地方添加 TODO 标记：

```typescript
// TODO: [Dev] 对接真实 API 接口
const fetchData = async () => {
  // 当前使用 Mock 数据
  return MOCK_DATA;
};

// TODO: [Dev] 处理表单提交逻辑
const handleSubmit = (data: FormData) => {
  console.log('提交数据:', data);
};
```

## 类型定义规范

### 1. 类型文件位置

- 全局类型定义：`src/types/index.ts`
- 功能相关类型：`src/types/[feature].ts`

### 2. 类型命名

- 接口使用 PascalCase：`UserProfile`
- 类型别名使用 PascalCase：`ApiResponse`
- 枚举使用 PascalCase：`UserStatus`

### 3. 导出规范

```typescript
// 使用 export interface
export interface User {
  id: string;
  name: string;
}

// 使用 export type
export type Status = 'pending' | 'completed' | 'failed';
```

## 路由规范

### 1. 路由配置

所有路由配置在 `src/router/index.tsx` 中：

```typescript
export const router = createBrowserRouter([
  {
    path: '/',
    element: <App />,
    children: [
      {
        path: 'example',
        element: <ExamplePage />,
      },
    ],
  },
]);
```

### 2. 页面文件位置

页面文件放在 `src/pages/[FeatureName]/index.tsx`

### 3. 路由跳转

使用 React Router 的 `useNavigate`：

```typescript
import { useNavigate } from 'react-router-dom';

const navigate = useNavigate();
navigate('/example');
```

## 其他规范

### 1. 注释规范

- 组件顶部添加 JSDoc 注释说明组件用途
- 复杂逻辑添加行内注释
- TODO 标记必须包含 `[Dev]` 前缀

### 2. 错误处理

- 使用 try-catch 处理异步操作
- 错误信息要清晰明确

### 3. 性能优化

- 使用 `React.memo` 优化组件渲染（如需要）
- 使用 `useMemo` 和 `useCallback` 优化计算和函数（如需要）

---

**记住：生成的代码应该看起来是"成品"，但内部结构要便于开发人员接手！**

