# 组件使用指南

本文档介绍如何使用项目中的 UI 组件和业务组件。

## 📚 目录

- [shadcn/ui 组件](#shadcnui-组件)
- [业务组件](#业务组件)
- [常用组件示例](#常用组件示例)

## shadcn/ui 组件

项目已集成 shadcn/ui 组件库，所有组件位于 `src/components/ui/` 目录。

### 可用组件列表

- **Button** - 按钮组件
- **Card** - 卡片组件
- **Input** - 输入框组件
- **Dialog** - 对话框组件
- **Table** - 表格组件
- **Form** - 表单组件
- **Select** - 选择器组件
- **Tabs** - 标签页组件
- **Badge** - 徽章组件
- **Avatar** - 头像组件
- 等等...

### 查看完整组件列表

访问 [shadcn/ui 官网](https://ui.shadcn.com/) 查看所有可用组件及其文档。

### 添加新组件

如果需要添加新的 shadcn/ui 组件，可以使用以下命令（在 Trae 编辑器中告诉 AI）：

```
请添加 shadcn/ui 的 [组件名] 组件
```

## 业务组件

### ExampleCard

跨页面复用的卡片组件示例。

```typescript
import { ExampleCard } from '@/components/biz/ExampleCard';
import { User } from 'lucide-react';

<ExampleCard
  title="标题"
  description="描述信息"
  icon={User}
  actionLabel="操作"
  onAction={() => console.log('点击')}
/>
```

## 常用组件示例

### Button 按钮

```typescript
import { Button } from '@/components/ui/button';

// 基础按钮
<Button>点击我</Button>

// 不同变体
<Button variant="default">默认</Button>
<Button variant="destructive">危险</Button>
<Button variant="outline">轮廓</Button>
<Button variant="ghost">幽灵</Button>

// 不同尺寸
<Button size="default">默认</Button>
<Button size="sm">小</Button>
<Button size="lg">大</Button>
```

### Card 卡片

```typescript
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from '@/components/ui/card';

<Card>
  <CardHeader>
    <CardTitle>卡片标题</CardTitle>
    <CardDescription>卡片描述</CardDescription>
  </CardHeader>
  <CardContent>
    <p>卡片内容</p>
  </CardContent>
</Card>
```

### Input 输入框

```typescript
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';

<div className="space-y-2">
  <Label htmlFor="email">邮箱</Label>
  <Input id="email" type="email" placeholder="请输入邮箱" />
</div>
```

### Dialog 对话框

```typescript
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle, DialogTrigger } from '@/components/ui/dialog';
import { Button } from '@/components/ui/button';

<Dialog>
  <DialogTrigger asChild>
    <Button>打开对话框</Button>
  </DialogTrigger>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>对话框标题</DialogTitle>
      <DialogDescription>对话框描述</DialogDescription>
    </DialogHeader>
    <p>对话框内容</p>
  </DialogContent>
</Dialog>
```

### Table 表格

```typescript
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';

<Table>
  <TableHeader>
    <TableRow>
      <TableHead>姓名</TableHead>
      <TableHead>邮箱</TableHead>
      <TableHead>操作</TableHead>
    </TableRow>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell>张三</TableCell>
      <TableCell>zhangsan@example.com</TableCell>
      <TableCell>
        <Button size="sm">编辑</Button>
      </TableCell>
    </TableRow>
  </TableBody>
</Table>
```

### Form 表单

```typescript
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import * as z from 'zod';
import { Form, FormControl, FormDescription, FormField, FormItem, FormLabel, FormMessage } from '@/components/ui/form';
import { Input } from '@/components/ui/input';
import { Button } from '@/components/ui/button';

const formSchema = z.object({
  username: z.string().min(2, {
    message: '用户名至少2个字符',
  }),
});

function MyForm() {
  const form = useForm<z.infer<typeof formSchema>>({
    resolver: zodResolver(formSchema),
    defaultValues: {
      username: '',
    },
  });

  function onSubmit(values: z.infer<typeof formSchema>) {
    // TODO: [Dev] 处理表单提交
    console.log(values);
  }

  return (
    <Form {...form}>
      <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-8">
        <FormField
          control={form.control}
          name="username"
          render={({ field }) => (
            <FormItem>
              <FormLabel>用户名</FormLabel>
              <FormControl>
                <Input placeholder="请输入用户名" {...field} />
              </FormControl>
              <FormDescription>这是你的公开显示名称</FormDescription>
              <FormMessage />
            </FormItem>
          )}
        />
        <Button type="submit">提交</Button>
      </form>
    </Form>
  );
}
```

### Badge 徽章

```typescript
import { Badge } from '@/components/ui/badge';

<Badge>默认</Badge>
<Badge variant="secondary">次要</Badge>
<Badge variant="destructive">危险</Badge>
<Badge variant="outline">轮廓</Badge>
```

### Tabs 标签页

```typescript
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';

<Tabs defaultValue="account" className="w-full">
  <TabsList>
    <TabsTrigger value="account">账户</TabsTrigger>
    <TabsTrigger value="password">密码</TabsTrigger>
  </TabsList>
  <TabsContent value="account">账户内容</TabsContent>
  <TabsContent value="password">密码内容</TabsContent>
</Tabs>
```

## 图标使用

项目使用 `lucide-react` 图标库：

```typescript
import { User, Settings, Home, Search } from 'lucide-react';

<User className="h-4 w-4" />
<Settings className="h-5 w-5 text-primary" />
```

查看所有可用图标：[Lucide Icons](https://lucide.dev/icons/)

## 提示

- 所有组件都支持 Tailwind CSS 类名自定义样式
- 组件都支持深色模式（通过语义化颜色变量）
- 遇到问题可以查看 shadcn/ui 官方文档

